pre-commit:
  parallel: false
  commands:
    check:
      run: mise exec -- just check
    clippy:
      run: mise exec -- just clippy

pre-push:
  parallel: false
  commands:
    test:
      run: mise exec -- just test
